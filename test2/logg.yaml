apiVersion: "logging.openshift.io/v1"
kind: "ClusterLogging"
metadata:
  name: "instance"
  namespace: openshift-logging
spec:
  managementState: "Managed"
  logStore:
    type: "elasticsearch"
    elasticsearch:
      nodeCount: 3
      storage: {}
      proxy:
        resources: 
          limits:
            memory: 500Mi
          requests:
            cpu: 100m
            memory: 256Mi
  visualization:
    type: "kibana"
    kibana:
      resources: 
        limits:
          memory: 300Mi
        requests:
          cpu: 100m
          memory: 100Mi
      replicas: 2
  collection:
    logs:
      type: "fluentd"
      fluentd:
        resources: 
          limits:
            memory: 500Mi
          requests:
            cpu: 100m
            memory: 256Mi
